╔══════════════════════════════════════════════════════════════╗
║  COPIA Y PEGA ESTE COMANDO EN TU TERMINAL (CMD O POWERSHELL) ║
╚══════════════════════════════════════════════════════════════╝

ssh root@74.208.69.243 "cat > /var/www/vhosts/system/api.tudestino.qipuh.com/conf/vhost.conf << 'FINALEOF'
ProxyPreserveHost On
ProxyPass / http://localhost:3000/
ProxyPassReverse / http://localhost:3000/
ProxyPass /socket.io/ ws://localhost:3000/socket.io/
ProxyPassReverse /socket.io/ ws://localhost:3000/socket.io/
FINALEOF
" && ssh root@74.208.69.243 "/usr/sbin/plesk repair web api.tudestino.qipuh.com && systemctl reload apache2 && curl -s http://api.tudestino.qipuh.com/health"


═══════════════════════════════════════════════════════════════

SI QUIERES EJECUTARLO PASO POR PASO:

Paso 1 - Conectar al servidor:
ssh root@74.208.69.243

Paso 2 - Crear archivo de configuración:
cat > /var/www/vhosts/system/api.tudestino.qipuh.com/conf/vhost.conf << 'EOF'
ProxyPreserveHost On
ProxyPass / http://localhost:3000/
ProxyPassReverse / http://localhost:3000/
ProxyPass /socket.io/ ws://localhost:3000/socket.io/
ProxyPassReverse /socket.io/ ws://localhost:3000/socket.io/
EOF

Paso 3 - Reconstruir configuración:
/usr/sbin/plesk repair web api.tudestino.qipuh.com

Paso 4 - Recargar Apache:
systemctl reload apache2

Paso 5 - Probar:
curl http://api.tudestino.qipuh.com/health

═══════════════════════════════════════════════════════════════

DEBERÍAS VER:
{"status":"OK","timestamp":"2025-10-23T..."}

Si ves eso, ¡TODO ESTÁ FUNCIONANDO! ✅
